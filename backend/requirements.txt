# ── Core API ────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0
python-dotenv==1.0.1
pydantic-settings==2.4.0
python-multipart==0.0.9

# ── Database ────────────────────────────────
sqlalchemy==2.0.35
psycopg2-binary==2.9.9

# ── Vector & Graph ──────────────────────────
qdrant-client==1.11.0
neo4j==5.24.0

# ── LlamaIndex (unpinned — let pip resolve compatible versions) ──
llama-index-core
llama-index-vector-stores-qdrant
llama-index-graph-stores-neo4j

# ── Document processing ─────────────────────
PyPDF2==3.0.1
python-docx==1.1.2
openpyxl==3.1.5
Pillow==10.4.0

# ── Azure storage ───────────────────────────
azure-storage-blob==12.22.0

# ── LLM & Agents ────────────────────────────
langchain==0.3.1
langchain-openai==0.2.1
langgraph==0.2.28

# ── Memory ──────────────────────────────────
redis==5.1.0

# EXCLUDED (too large / not needed in production):
# torch, transformers, accelerate, spacy, sentence-transformers
