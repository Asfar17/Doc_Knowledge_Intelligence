# Azure deployment — disabled in favour of Vercel + Render
# Re-enable by removing the 'if: false' conditions when Azure 409 issue is resolved

name: Deploy to Azure (disabled)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type DEPLOY to confirm Azure deployment'
        required: true

jobs:
  placeholder:
    name: Azure deployment disabled
    runs-on: ubuntu-latest
    if: github.event.inputs.confirm == 'DEPLOY'
    steps:
    - run: echo "Azure deployment manually triggered"
